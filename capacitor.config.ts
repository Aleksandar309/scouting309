import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.wanderingaxolotltrot',
  appName: 'wandering-axolotl-trot',
  webDir: 'dist'
};

export default config;
